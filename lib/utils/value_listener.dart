import 'package:flutter/material.dart';

class ValueListener {
  static ValueNotifier<bool> idDark = ValueNotifier<bool>(true);
}

//Definir clase valuenotifier
//1. definir la clase
//2. definir donde se va a utilizar
//3. construir el builder para realizar el cambio de variable